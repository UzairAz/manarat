<odoo>
    <data>
        <record id="partner_ledger_report_form" model="ir.ui.view">
            <field name="name">partner.ledger.report</field>
            <field name="model">partner.ledger.report</field>
            <field name="type">form</field>
            <field name="arch" type="xml">
                <form>
                    <group>
                        <group>
                            <field name="date_from"/>
                            <field name="to_date"/>
                            <field name="journal_ids" widget="many2many_tags"/>
                        </group>
                        <group>

                        </group>
                        <footer>
                            <button name="print_report" context="{'from_wizard': True}" string="Print Report" type="object" class="oe_highlight"/>
                            <button string="Cancel" class="btn btn-default" special="cancel"/>
                        </footer>
                    </group>
                </form>
            </field>
        </record>
        <record id="action_partner_leadger" model="ir.actions.act_window">
            <field name="name">Partner Ledger Report</field>
            <field name="res_model">partner.ledger.report</field>
            <field name="type">ir.actions.act_window</field>
            <field name="view_mode">form</field>
            <field name="view_id" ref="partner_ledger_report_form"/>
            <field name="target">new</field>
        </record>
    </data>
</odoo>